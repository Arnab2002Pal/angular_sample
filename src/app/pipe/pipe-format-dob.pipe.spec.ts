import { PipeFormatDobPipe } from './pipe-format-dob.pipe';

describe('PipeFormatDobPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFormatDobPipe();
    expect(pipe).toBeTruthy();
  });
});
